repository:
  storage:
    patterns:
      - name: storage.type.function.saturnus
        match: \b(fn)\b
      - name: storage.type.saturnus
        match: \b(let)\b
      - name: storage.type.saturnus
        match: \b(class)\b
